/*
Faça um programa que inclua:
     
Uma função void leitura (int vet [] , int dim) que faça a leitura de 10 valores (inteiros), guardando-os num vetor(nota dim representa a dimensão do vetor que neste caso é 10); 

Uma função int maiorDiferenca (int vet [] , int dim) que retorne a diferença entre o maior e o menor valor do vetor;

Uma função int contaPares (int vet [] , int dim) que devolva o número de valores pares e ímpares do vetor;
*/

#include<iostream>
//O Sporting é o Maior (depende dos dias).

using namespace std;

int i, j=0, w;

void leitura(int vet[i], int dim)
		cin>>vet[i];
		
int maiorDiferenca(int vet[i], int dim)
	{
		for(i=0,i>10,i++)
		
		if(i>=j)
		cout<< i;
		else
		cout<< j;
	}
	w=i-j;
	
int contaPares(int vet[i], int dim)
	{
		for(i=0;i>10;i++)
		
		if(i%2==0)
		cout<<i<<" e par\n";
		else(i%2!=0)
		cout<<i<<" e impar\n";
	}
	
	int main()
	{
		cout<<leitura(int vet[i],int dim);
		cout<<maiorDiferenca(int vet[i], int dim);
		cout<<contaPares(int vet[i],int dim);
	}
